# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma

# tài liệu skill AI tự cài bởi `prisma init` (Prisma 7), không phải code dự án
/.agents/
/.claude/
/.windsurf/
/skills-lock.json

# file kế hoạch do công cụ AI sinh — cùng lý do với khối trên và với việc đã xóa
# CLAUDE.md/AGENTS.md: repo trên GitHub không mang dấu vết công cụ AI
/docs/superpowers/

# ảnh gốc chủ dự án gửi. Đã copy đủ sang `public/images/` (chỗ app thật sự đọc);
# 29 file ở đây trùng byte-for-byte với bản trong `public/images/`, kể cả 5 file
# `promo-*v2.jpg` chỉ khác tên. Commit vào là +61MB trùng lặp vĩnh viễn trong
# lịch sử git. Giữ trên máy làm bản gốc, đừng đưa lên repo.
/img/
